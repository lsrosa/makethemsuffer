void loop3(float * a, int * p, float * b, int * m){
  float temp;
  int n = *p;

  for(int i=0; i<=n; i++){
    for(int j=0; j<=n; j++){
      temp = 0;
      for(int k=0; k<=n; k++){
        temp += a[i*n+k]*a[k+j*n];
      }
      b[i*n+j] = temp;
    }
  }

  *m = n;

  return;
}
